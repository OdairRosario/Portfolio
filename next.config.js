/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    swcMinify: true,
    //output: 'export',
  //  assetPrefix: '/my-next-app/'
  }
  
  module.exports = nextConfig